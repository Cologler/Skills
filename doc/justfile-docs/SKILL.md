---
name: justfile-docs
description: Use this skill for questions about using just and justfiles, including writing or reviewing recipes, variables, parameters, dependencies, settings, shell behavior, Windows paths, modules, imports, CLI options, and migration from Make or package scripts. This skill routes agents to the bundled official just usage documentation in references/docs and should be used whenever a task depends on exact just syntax or behavior.
---

- Homepage: <https://github.com/casey/just>
- Version: bundled official usage documentation snapshot generated from the just repository
- Last updated: 2026-06-16
- This document is generated by gpt-5.5

# Justfile Docs

This skill helps agents answer questions and make changes involving `just`,
`justfile`, and Just recipes by using the bundled official usage documentation
in `references/docs`.

The skill body is intentionally a guide to the references, not a replacement
for them. If the answer depends on exact syntax, edge-case behavior, or command
semantics, read the relevant reference file before answering or editing code.

## When to use this skill

Use this skill when the user asks about:

1. Writing, editing, reviewing, or debugging a `justfile`.
2. Recipe syntax, parameters, dependencies, aliases, groups, attributes, private
   recipes, default recipes, or documentation comments.
3. Variables, assignments, constants, expressions, substitutions, strings,
   conditional expressions, or built-in functions.
4. Shell configuration, shebang recipes, script recipes, backticks, working
   directories, environment variables, or signal and error behavior.
5. Imports, modules, global or user justfiles, fallback to parent justfiles,
   remote justfiles, markdown justfiles, or hidden justfiles.
6. The `just` CLI, including command-line options, listing recipes, formatting,
   dumping, completion scripts, parallelism, timestamps, or watch-style reruns.
7. Platform-sensitive behavior such as Windows paths, argument splitting, shell
   aliases, Node.js `package.json` script compatibility, Python recipes with
   `uv`, or safer Bash shebang patterns.
8. Comparing just with Make, Cargo build scripts, shell scripts, or other prior
   art.

## How to use the references

Start from the user's concrete task, then open only the reference files needed
to resolve that task. The bundled docs are the authoritative reference for the
just usage topics they cover. Do not fetch external docs for just behavior
already covered by `references/docs`.

Find relevant references with `rg` instead of relying on a hand-maintained
topic map. Search `references/docs` for the user's exact terms first, then try
nearby just terminology if needed.

Example searches:

```bash
rg -n "shebang|script recipe|shell" references/docs
rg -n "parameter|argument|dependency" references/docs
rg -n "windows|path|working directory" references/docs
```

Read the matching files that actually explain the behavior before answering or
editing. If search results are broad, prefer files whose names match the topic
and then confirm the details in the body text.

## Working rules

When using this skill:

1. Prefer bundled references over memory for exact just behavior.
2. Read references before making syntax-sensitive claims or edits.
3. Keep answers grounded in the official docs and say which reference files
   informed the result when that helps the user verify it.
4. If the user asks for a justfile change, inspect the existing file first and
   preserve its style, shell assumptions, and line endings.
5. Do not use this `skill.md` alone as proof of just syntax or semantics. It is
   only the routing layer for the generated official references.

## Scope of bundled docs

The generated references are intentionally not the full just book. The
generation recipe removes installation, editor support, contributing,
changelog, backwards compatibility, and summary material before copying docs
into `references/docs`.

Use this skill for how to use just: syntax, recipes, settings, command-line
behavior, shell behavior, modules, imports, paths, environment handling,
portability, and comparisons. For omitted project-maintenance topics, recognize
that the bundled references may not contain the answer.

## Further reference

The bundled references in `references/docs` are the official just usage docs
snapshot for this skill. They cover the just language, command-line interface,
recipes, settings, shell behavior, platform notes, and comparisons this skill
intentionally omits from the main body to stay lean.

Do not fetch the homepage or other online documentation for usage information
already covered by `references/docs`.
